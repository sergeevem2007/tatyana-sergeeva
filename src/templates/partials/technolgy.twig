<section class="technology pb-6 pb-lg-10">
	<div class="container">
		<div class="row">
			<div class="col-12">
				<h2 class="fs-24 fs-lg-48 text-secondary mb-45 mb-lg-6">Проверенная технология <br> из 12 консультаций, на которых ты:</h2>
			</div>
		</div>
		<div class="row gx-lg-5 gy-4">
			{% for tech in site.technology %}
				<div class="col-12 col-md-6 col-lg-4">
					<div class="technology__card d-flex flex-column justify-content-stretch p-4 rounded-4 shadow h-100">
						<div class="technology__icon">
							<img src="{{ tech.img }}" alt="" width="40">
						</div>
						<p class="fs-16 fs-lg-20 mb-45">{{ tech.desc }}</p>

						<div class="d-flex justify-content-center mt-auto">
							<a href="#{{ tech.idModal }}" data-bs-toggle="modal" data-bs-target="#{{ tech.idModal }}" class="btn btn-outline-secondary fw-700 text-primary text-uppercase rounded-pill px-5 py-3">Пример</a>
						</div>
					</div>
				</div>
			{% endfor %}
		</div>
	</div>
</section>

{% for tech in site.technology %}
	<div class="modal" id="{{ tech.idModal }}" tabindex="-1" aria-labelledby="{{ tech.idModal }}Label" aria-hidden="true">
		<div class="modal-dialog">
			<div class="modal-content">
				<div class="modal-header px-md-45">
					<h5 class="modal-title">Разбор ситуации на примере клиентки</h5>
					<button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
				</div>
				<div class="modal-body px-md-45">
					{{ tech.exampleText }}
				</div>
			</div>
		</div>
	</div>
{% endfor %}
